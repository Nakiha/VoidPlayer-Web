# Roadmap

2026-09-07 确定。实施顺序：**3 部署基础 → 1 媒体库 → 2 管理后台**，随后完成首个正式 release 的验收。本文记录计划，未勾选项尚未完成。

## 已确定的交付方向

- 用 Bun `build --compile` 将服务端及运行时打成独立可执行文件，最终用户无需安装 Node、Bun 或 npm 依赖。
- 发布包包含 `voidplayer` 可执行文件（Windows 为 `.exe`）、构建后的网页/WASM 资源、配置示例和许可证。首版保留资源目录，不要求所有资源塞进单个二进制。
- 配置、索引数据库、日志及后续上传内容放在独立可写数据目录；程序升级不覆盖用户数据。
- 本地与远端运行同一套服务。开发、前端构建和现有测试可以继续使用 Node/npm，发布运行时采用 Bun。
- 为支持的操作系统和架构分别构建、验证产物；具体首发平台矩阵在部署阶段确定，不把交叉编译成功当作目标平台验收通过。

## 阶段 3：可部署预览版

目标：脱离开发仓库，在实际服务器或本地机器上解压并启动，先发布 prerelease。

- [x] 验证 Bun 下的 HTTP/HEAD/Range、流式读取与中断、文件系统、进程信号及正常退出；保留现有会话和解码架构。三平台独立产物已通过；Linux/macOS 验证 SIGTERM，Windows 额外验证独立控制台 Ctrl+C 正常退出。
- [x] 固定 Bun 1.4.2 并生成服务端可执行文件，明确程序目录、资源目录、配置路径和数据目录，支持从任意工作目录启动。
- [x] 完成首次配置、目录权限和启动错误提示，以及健康/就绪检查。
- [x] 验证本地访问、远端认证入口、HTTPS、视频所需响应头和媒体读取；Linux 从随包 Docker/Compose/Caddy 模板验收。
- [x] 打包网页与两种 WASM core，携带本项目和依赖许可证、应用源码快照与精确构建来源说明。
- [x] 生成可追溯的版本信息、产物清单和校验和，建立 GitHub Actions 自动构建与产物测试流程。
- [x] 明确启动、停止、后台运行、升级、备份和恢复方式；三平台验收升级保留配置/日志、完整数据目录备份恢复及缺失资源的升级拒绝。见 [后台运行与恢复](../deploy/operations.md)。
- [x] 在没有 Node/Bun、没有源码与 node_modules 的干净环境验收，并以生成的真实可解码媒体验证独立部署。前半项已通过 Linux 镜像和三平台空 PATH 测试，本轮已使用 FFmpeg 生成的多根目录、深层嵌套和零散片源环境完成验收。

本阶段先使用现有媒体库能力验证部署。只预留管理入口，不提前铺开完整 admin 页面。

## 阶段 1：媒体库重做

目标：可靠管理多个物理或网络存储根目录下的大量嵌套视频文件，播放不受全库扫描阻塞。

- [x] 明确稳定的根目录/媒体 ID、内容版本、目录层级及旧工作区引用的兼容或迁移方式。首次配置写入稳定根 ID，媒体 URL 固定元数据版本；旧工作区按已存大小/修改时间核对后迁移，别名与最近记录保留兼容。旧记录不含版本时无法追溯历史字节，限制已记录。
- [x] 使用持久化索引，数据库存放在服务端本地数据目录；重启后可查询上次结果。SQLite schema 1 已在三平台独立产物上验证，包括停机备份后媒体离线时恢复缓存。
- [x] 后台扫描与媒体读取分离，提供扫描进度、错误、取消与恢复能力。状态和控制 API 已实现；恢复从根目录重新校准已存索引。扫描停滞时四路 HTTP Range 不等待扫描，崩溃后进程锁释放。
- [x] 目录浏览、面包屑、分页和当前目录/全库搜索，替代一次返回并渲染整个媒体库。UI 与 Agent 均使用有界分页，集合变化以 revision 明确失效并返回第一页；最近记录独立查询，不依赖当前页。WebKit/Chromium 交互与亮暗外观已验证。
- [x] 处理深层目录、大量文件、文件写入中/替换/删除和权限不足，不再静默漏项。5100 文件、12 层目录和各状态已有后端回归；UI 展示待写入/离线/扫描错误数量，版本改变阻止旧工作区还原。真实网络挂载另行验收。
- [x] 网络存储首版使用操作系统已挂载的路径；离线时保留索引并标记状态，重连后校准。RC 3 已补上文件系统类型改变后露出空目录的保护，以及旧索引首次绑定前核对原媒体；本轮用生成片源目录的移走/恢复验证离线流程，实际 SMB/NFS 另行验收，不作为首版发布门槛。
- [x] 增量更新、手动刷新与周期校准配合。目录提示合并为局部扫描，监听最多 512 个句柄，超限/不可用退回周期校准；初次注册补查、目录身份更换、取消排队任务和写入稳定复查均已验证。文件监听仅作提示，不作为网络存储完整性的唯一依据。
- [x] 验证扫描期间多轨 Range 请求、断线恢复、服务重启及现有工作区导入。

详细候选模型见 [媒体库演进方案](media-library-evolution.md)。收藏、服务器评审集合等扩展能力不自动成为本阶段必做项。

## 阶段 2：服务端管理后台

目标：围绕实际的数据和任务提供管理能力，与播放器共用服务端接口。

- [x] 独立 `/admin` 入口；播放器连接状态入口在新标签页打开服务端管理界面。WebKit/Chromium 已验证独立入口、主题同步和响应式布局。
- [x] 管理身份、访问权限和写操作边界。本机要求回环连接与 Host；远端沿用可信网关身份及显式管理员名单，写操作校验同源与动作头。
- [x] 管理媒体根目录与扫描任务，查看不可读目录、离线存储和索引状态。根目录保留稳定 ID，配置带版本校验并原子写回；外部修改或启动参数覆盖时暂停后台保存，更新后立即校准索引。
- [x] 展示服务版本、运行状态、连接、资源使用及诊断日志。日志提供有界分页、JSON 预览、下载和按版本删除，上传归属由服务端记录；本次运行请求记录保留最近 200 条。
- [x] 分别测量客户端到服务端吞吐、存储读取能力和并发服务能力，明确测试条件与耗时；主动测速由用户触发。提供内存下载、上传、单路存储及四路媒体读取，显示浏览器/服务器计时来源、数据量和缓存影响；任务受时长、数据量、并发数限制并支持取消。
- [x] 定义端侧上传/编辑对象的持久化、归属、版本与冲突处理，再提供对应的检视和管理页面。首版为主动上传的诊断日志与主动保存的工作区；工作区独立 SQLite 持久化，归属由服务器生成，版本匹配后才能更新/删除，冲突保留本地会话并可另存副本。播放器和管理页均已接入。

上传/编辑范围为诊断日志和工作区，不包含通用文件管理器、视频上传或实时协作。工作区当前版本可编辑，版本号用于冲突检测，不承诺完整版本历史；客户端内容仍只在用户主动上传或保存时发送。

## 首个正式 release 验收

- [x] 用 FFmpeg 生成的多根目录、深层嵌套和零散片源完成端到端评审。
- [x] 在声明支持的平台验证安装、首次启动、重启、升级、数据迁移与备份恢复。RC 3 的三平台原生产物通过初始化、程序目录升级、索引 schema 1 → 2 迁移、空挂载点下保留缓存、重连，以及配置/索引/工作区备份恢复；兼容已有媒体引用和配置，未知数据库版本拒绝打开，不会静默重置。
- [x] 验证资源齐全、许可证齐全、远端访问边界及升级失败后的恢复路径。原生归档核验逐文件摘要与来源/许可证，缺失资源阻止升级；Linux 随包 HTTPS/容器验证身份与只读部署，旧程序和完整数据备份可用于恢复。
- [x] 从发布产物运行回归，而不只测试源码启动；发布已知限制和操作文档。RC 3 的 macOS ARM64 Actions 归档通过 WebKit/Chromium 双轨、标注、文件导入/导出、服务器工作区和重启恢复；同包 WebKit 四组播放基准通过。操作文档随包提供，[候选版限制](releases/0.1.0-rc.3.md) 已记录。其他平台的浏览器显示效果仍不扩大声明为已验收。

## 当前状态

部署基础、媒体库和管理后台已落地，当前推进正式发布验收。发布脚本使用独立可执行文件加资源目录。macOS ARM64 预览产物通过空 PATH、与源码无关的工作目录、归档校验、初始化、并发 Range、中断、网关身份和更换程序目录保留配置/日志测试；独立服务上的四组 WebKit 播放基准通过。

正式打包入口已切换为 GitHub Actions：主分支代码推送、版本标签或手动触发，固定 core 构建仓库、FFmpeg、dav1d 修订和工具链，从源码准备同一份解码器，再由 Linux x64、Windows x64、macOS ARM64 原生 runner 打包并测试。分支预览产物与校验和保留 30 天；版本标签经三平台验证和汇总后创建 Release 草稿，公开发布仍需完成正式验收。详见 [发布与校验](../deploy/README.md#发布与校验)。[三平台独立产物、备份恢复及部署验证已通过](https://github.com/Nakiha/VoidPlayer-Web/actions/runs/34053905776)，对应应用修订 `b97915d`。Linux 额外验证随包 Docker/Compose/Caddy 的非 root、只读运行、无 Node/Bun、可信 TLS、认证边界、并发 Range 与重启后保留 CA；Windows 已验证真实独立控制台 Ctrl+C 正常退出。这是部署基础阶段的历史验收；后续媒体库/管理后台记录见下方。签名与公证尚未提供，真实网络存储仍待验收，不标记整个 roadmap 完成。

首个独立部署预览版 [v0.1.0-preview.1](https://github.com/Nakiha/VoidPlayer-Web/releases/tag/v0.1.0-preview.1) 已发布。三平台归档和校验文件的 GitHub SHA-256 与本地核验过的 Actions 产物一致。版本范围和已知限制见 [预览版记录](releases/0.1.0-preview.1.md)。阶段 1 的持久化索引与后台扫描已落地，真实存储验收继续保留为未完成项。


2026-09-07：媒体索引基础提交 `fbb7a50`，批量写入与 Windows 测试清理修正 `3202a33`。[三平台新索引与独立部署验收通过](https://github.com/Nakiha/VoidPlayer-Web/actions/runs/34056688978)。Node/Bun 均验证 5100 文件与 12 层目录、稳定根 ID、旧 URL 别名、文件版本变更、取消/崩溃恢复和扫描期间四路 Range；权限错误测试在 Linux/macOS 验证，Windows 不模拟 POSIX 权限。独立包验证 SQLite 锁与完整备份后的离线索引；Linux 容器验证持久化索引数据卷。全套本地测试 163 项通过，构建通过。

2026-09-07：目录浏览与版本引用提交 `06738ce` / `10c9881`。UI 延续斑马色列表、统一控件与主题变量；提供根目录选择、面包屑、60 项分页和搜索范围。新增 metadata 接口用于最近记录和旧工作区迁移；下载/定位 URL 正确保留版本。WebKit 与 Chromium 验证翻页、扫描变更提示、跨目录搜索、深层最近记录、存储离线/恢复、带版本下载和同大小/mtime 替换拒绝；原有 WebKit 片源/轨道和工作区回归通过。本地全套 170 项测试通过，构建通过。[最新三平台原生产物验收通过](https://github.com/Nakiha/VoidPlayer-Web/actions/runs/34058536322)，对应 `fc5c468`；Bun 使用显式释放语句的 SQLite 关闭路径，Windows 额外验证仍持有语句引用时数据库文件可以关闭并移动。

2026-09-07：增量更新提交 `f19a8f4`，[三平台原生监听与独立发布包验证通过](https://github.com/Nakiha/VoidPlayer-Web/actions/runs/34059755626)。局部扫描不会清理兄弟目录；扫描中途的变化排队合并，取消同时清理排队任务。监听仅覆盖已验证目录，保留周期校准及 `indexWatch: false` 回退；Node 全套 180 项与本机 Bun 索引 20 项测试通过，构建及 macOS 独立包验证通过。Actions 同时验证 Linux/Windows/macOS 的 Node/Bun 索引与编译后原生监听，Linux 部署模板与 Windows 控制台退出继续通过。

2026-09-07：服务管理首批功能提交 `ab2e8ec`，[三平台原生发布包与 Linux Docker/HTTPS 管理验收通过](https://github.com/Nakiha/VoidPlayer-Web/actions/runs/34061877658)。管理页沿用现有主题与斑马色列表；WebKit/Chromium 验证目录保存、离线状态、日志预览/下载/删除、跨标签主题同步及 390/720/1200 像素布局，原有播放器与主题回归通过。Node 全套 186 项、本机 Bun 管理/配置 11 项测试通过。编译产物验证管理身份和版本冲突；Linux 容器将配置初始化到持久化 `/data`，验证非 root、只读程序目录下可保存，重启后配置不被模板覆盖。使用方式见 [服务管理](../deploy/admin.md)。

2026-09-07：主动测速提交 `f1c689f`，[三平台原生与 Linux HTTPS 测速验收通过](https://github.com/Nakiha/VoidPlayer-Web/actions/runs/34063344994)。本地 Node 全套 195 项、本机 Bun 测速 9 项测试通过；WebKit/Chromium 验证四种模式、媒体选择、取消和亮暗响应式布局。原生产物及随包 Caddy 网关验证四种实际读取/传输路径。回归覆盖管理员身份、单任务互斥、时限与数据量限制、非整 MiB 文件循环、普通 Range 并行读取，以及停滞上传取消后连接和名额及时释放。此测试测量当前路径的有效吞吐，不声明理论硬件上限；存储读取包含系统缓存。配置与解释见 [主动测速](../deploy/admin.md#主动测速)。

2026-09-07：服务器工作区提交 `695139a`，[三平台持久化工作区与 Linux HTTPS 部署验收通过](https://github.com/Nakiha/VoidPlayer-Web/actions/runs/34064754153)。播放器与管理页使用同一工作区格式；WebKit/Chromium 验证真实双轨、标注、双窗口编辑冲突、另存副本、载入最新版本、管理员改名/下载/按版本删除，以及服务重启后的会话恢复。原有文件导入导出、载入失败回滚、本地重新连接与取消继续通过。Node 全套 201 项、本机 Bun 工作区 6 项测试通过；原生产物验证程序升级和停机备份恢复后内容、归属、版本完整，Linux 通过 Caddy 验证保存和重启保留。用户工作区存于独立 `workspaces.sqlite`，不随媒体索引重建删除。说明见 [服务器工作区](../deploy/admin.md#服务器工作区)。

2026-09-07：标签发布流程提交 `335e3d7`，精确版本验收修正 `027aca9`。[RC 2 的完整标签构建、三平台原生验收、Linux HTTPS 部署、产物汇总和 Release 草稿创建通过](https://github.com/Nakiha/VoidPlayer-Web/actions/runs/34065823788)。RC 1 曾被旧验收脚本中固定要求 preview 的断言拒绝，保留失败标签记录，未发布；RC 2 改为核对清单中的完整版本和修订。汇总确认同一修订、运行时和解码器字节，上传后验证 GitHub 的六个附件摘要。实际重跑草稿任务后 Release ID、附件 ID/摘要完全不变，仍为未发布 prerelease 草稿。公开的 Preview 1 保持不变。

2026-09-07：原生包浏览器验收脚本 `2f0bca4`。从 Actions 下载的 RC 2 macOS ARM64 包（修订 `027aca9`）直接启动，WebKit/Chromium 验证真实 AV1/FFV1 双轨、矢量标注、gzip 文件还原、偏移/位置/视图/布局、服务器保存与管理检视、进程重启后恢复；服务使用空 PATH、独立临时数据和端口，网页逐字节匹配归档，不使用仓库 dist。WebKit 的 HEVC 4K、VVC WASM、VVC+HEVC、MPEG-2 TS+H264 四组播放基准通过。复验命令见 [从发布包运行浏览器验收](../deploy/README.md#从发布包运行浏览器验收)。

2026-09-07：存储保护提交 `701163c`，[RC 3 的三平台原生迁移与完整发布草稿流程通过](https://github.com/Nakiha/VoidPlayer-Web/actions/runs/34066927218)。修复网络挂载消失后露出可读空目录时，扫描可能把旧媒体校准为已删除的问题；索引 schema 2 保存根目录文件系统类型，变化时保留缓存、拒绝读取，恢复后重新核对。schema 1 原位升级保留媒体记录与别名，未核对到一个版本未变的原媒体前不绑定残留目录。新增 6 项回归覆盖重启、扫描中途类型变化、正常删除、显式迁移路径和旧库未核对数据不可在下一轮变成可信数据；本地 Node 全套 212 项、Bun 存储 6 项通过。三平台独立程序验证 schema 1 → 2 和空挂载点保护，Linux HTTPS/容器继续通过。下载的 RC 3 macOS 包通过 WebKit/Chromium 工作区回归和同包四组 WebKit 播放基准。文件系统类型模拟不是实际 NAS 测试，且不能区分同类型的另一块存储或根目录下独立的子挂载，限制见 [存储离线与根目录绑定](../deploy/admin.md#存储离线与根目录绑定)。

2026-09-07 验收范围调整：按用户要求，本轮不接入 SMB/NFS。使用 FFmpeg 生成的真实可解码视频构造多根目录、12 层嵌套、目录中零散文件和超过 5,000 个媒体条目，作为首版端到端验收环境。目录移走/恢复只模拟路径离线，不等同网络协议、挂载行为或吞吐实测；实际 SMB/NFS 验证延期，不再阻塞本轮发布。此前候选版关于真实存储待验收的记录保留为历史。

生成环境验收通过：9 个完整解码验证的母片复制/分布成 5,242 个真实媒体文件，3 根目录、12 层、每根 260 项分页目录。RC 3 Actions 的 macOS ARM64 归档在空 PATH 下通过 WebKit/Chromium：全部路径与摘要、同名 ID 隔离、分页搜索、扫描期间四路字节精确 Range（各 100 轮）、双轨与矢量标注、gzip 导入/导出、服务器保存、离线期间重启后保留索引、其他根可读、恢复后的工作区还原，以及替换文件后旧版本返回 409。Node 全套 212 项与构建通过。复现方式见 [生成片源验收环境](generated-library-acceptance.md)。

- [ ] 发布首个正式版本 v0.1.0：等待该标签的三平台构建、归档验收和草稿核验，确认后公开发布。
