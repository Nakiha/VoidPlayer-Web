# Annotation rendering and macOS Preview design QA

Source visual truth: `/var/folders/j5/x39dhv0n7h720pfxwzx7_mw00000gn/T/codex-clipboard-5ed132be-7f02-4d99-9214-95b2cb9feeff.png`.
Implementation screenshot: `/tmp/voidplayer-preview-style.png` (temporary QA artifact, not committed).
Implementation: http://127.0.0.1:5180/.

## Comparison scope and normalization

The source is a 1356 × 338 cropped macOS Preview screenshot; its capture density is not supplied. The implementation is 2816 × 1578 at 1408 × 789 CSS pixels, DPR 2. Compare the selected rectangle, eight handles and markup controls at equivalent screen scale, not the different media content or the native window chrome. Both images were opened together in one comparison input. The full app and focused editing state were also inspected through the in-app browser at 1280 × 720. This is an adaptation of Preview editing to the existing video-review workflow, not a replica of Preview's PDF/signature/crop features.

## Comparison history

1. P1: CSS-scaled annotation layer blurred strokes and selection controls at high zoom. Fixed by moving the SVG out of the transformed image wrapper and mapping coordinates through its viewport viewBox. Visible outlines and typed text were rechecked at 5×; the reported 3.135–12.988× sequence was replayed at DPR 2.
2. P1: Background grid composited over the video. Fixed explicit presentation/annotation stacking; native source canvas uses visibility:hidden rather than a scaled transparent compositor layer. Final screenshot pixels match presentation pixels, and hiding the background grid leaves the video crop byte-identical.
3. P2: Toolbar crossed into the source panel and displayed a redundant explanatory row. Moved under the viewport, bounded dragging and style-menu placement, single-row translucent surface with hover tips.
4. P2: Oversized square handles and heavy blue dashes differed from Preview. Replaced by 9 CSS-pixel blue circles with white rims, 18-pixel hit targets, eight resize positions and a fine black/white outline. Final screenshot compared with the supplied Preview reference shows consistent screen-sized handles and thin outlines.
5. P2: Form-like style selects cluttered the toolbar. Replaced with compact icon menus using shared menu behavior; tested selecting thin stroke and single-edge resizing. Fixed a reentrant tooltip popover error observed during the manual test.

6. P1 (follow-up): Source-normalized stroke widths caused rectangles created at different zooms to have different visual weights. Added explicit CSS-pixel `strokeWidth` and SVG non-scaling strokes. Legacy values display at 4px because their creation scale was not recorded. Regression creates at 1×/5×/10× and measures final screenshot ink, not only DOM attributes.
7. P1 (follow-up): Source-boundary clipping and coordinate clamps cut off shapes and handles. Removed the video clip and [0,1] clamps; the annotation SVG covers its track viewport. Negative/outside source coordinates survive movement, resize, autosave, reopening and export. The updated screenshot shows the selected rectangle above and below the small video, with all eight circles visible.
8. P2 (follow-up): The old first-drag region path still showed the thick blue rectangle. Removed that duplicate SVG/path. The editor now receives the initial drag directly and displays the same eight circular handles during creation. A 3px threshold preserves plain-click behavior.
9. P2 (follow-up): Thin edges were difficult to pick. Added invisible 18px outline targets plus interior selection for rectangles/ellipses. Drawing tools continue to create nested objects; selection mode moves them. Checked inside clicks and clicks 7px beyond a thin edge.

10. P2 (follow-up): Narrow toolbar horizontal pan exposed the native scrollbar across the controls. Hidden both standard and WebKit scrollbar rendering while preserving horizontal overflow, touchpad scrolling and keyboard reveal; the 480px test asserts real scrollLeft movement and a zero-height scrollbar gutter. Screenshot `/tmp/voidplayer-toolbar-narrow.png` was inspected after scrolling. The Chromium regression waits for resize/layout frames before positioning the pointer; sending wheel input during toolbar repositioning had intermittently targeted the old location. Both browser runs passed after that synchronization.
11. P2 (follow-up): Drawing-mode hit regions incorrectly advertised a move cursor, while click and drag were rigidly separated by tool. Hit regions now inherit drawing cursors; move cursors apply only to actual move actions. Pressing an object in drawing mode waits for intent: a precise tap selects and switches tool with no mutation/history entry; movement of 3 CSS pixels starts drawing from the initial point. Rect/ellipse/line/pen taps, nested drawing, subsequent movement, text editing, erasing and undo are checked in both browsers. In-app manual testing confirmed the crosshair-to-move transition and subsequent movement.

12. P2 (follow-up): Shared menu invokers reopened after the browser light-dismissed them, and tooltips covered open menus. Remembered pointer-press state fixes second-click close; all tooltip entry/timers are suppressed while a shared menu is open. Both WebKit and Chromium tests exercise repeated pointer and keyboard toggles, menu switching, Escape and outside dismissal. In-app pixel-mode toggling was verified.
13. P2 (follow-up): Stroke selectors used prose rather than visible weights and the native color picker dominated the viewport. Toolbar and menu now share actual line samples for 1/2/4/6/8/12px. A square color swatch opens one fixed strip of 12 common colors and a 12 × 10 grayscale/hue palette, matching the supplied two-section reference. Color cells join without gaps and have no visible labels. It does not track recent colors or persist preferences; the prior recent-color key is removed. In-app screenshots verified immediate selected-object updates, red defaults, the contiguous palette, and no overlapping tooltip. Temporary QA captures are `/tmp/voidplayer-stroke-menu.png` and `/tmp/voidplayer-color-menu.png`; no image assets are added to the project.

14. P2 (follow-up): Selected palette cells had an inset frame, and the toolbar color control was oversized with a clipped external focus ring. Replaced palette selection/hover/keyboard focus with a 32px white rounded floating surround around a rounded 20px color tile. Hit cells remain a gapless 20px grid, and the floating surround does not intercept adjacent cells. Toolbar ink is now 18px and toolbar button focus outlines are inset. Both browser regressions passed; `/tmp/voidplayer-color-feedback.png` and in-app keyboard focus were visually inspected. Hover changes no annotation data.

## Fidelity surfaces

- Typography: existing system UI font, compact 13px controls, restrained gray labels; editable text remains live content on the image.
- Spacing/layout: one row, grouped tools, thin separators, constrained viewport placement; no overlap with the right source panel in the tested desktop state.
- Colors/tokens: shared macOS-blue accent; Preview-like neutral icons, white-rim blue handles, fine contrasting selection edges, translucent chrome. Reduced-transparency theme continues to use opaque surfaces.
- Image quality/assets: videos retain their real decoded pixels; bilinear reduction and nearest magnification remain intact. SVG annotation coordinates remain vector geometry. Existing Phosphor icons are used; no bitmap control placeholders or generated artwork.
- Copy: explanatory row removed; instructions are tooltips. Save status remains screen-reader-only. All visible tools correspond to working app features.

## Validation

138 Node tests; WebKit and Chromium interaction regressions; DPR 2 AV1 + FFV1 log replay with final-composite pixel checks, grid occlusion and constant handle sizes; existing WebKit layout/source tests; four WebKit playback benchmarks passed. Manual in-app browser checks covered rectangle creation, text input, style menu selection, one-edge resize, toolbar placement and console errors (none after repair). Follow-up manual checks in the final build covered plain click, first drag, nested rectangle creation, interior movement, and selection extending above the video. WebKit and Chromium both passed the workspace regression after the final gesture change.

The uploaded Safari 27 log contained no decoder failure. Its exact compositor implementation is not identical to Playwright WebKit; the original transient blank-left-video symptom was not independently reproduced. The revised layer arrangement and final-pixel regression pass for the reported sequence. Do not claim all Safari versions are certified.

The earlier pass missed the first-drag and cross-zoom/outside-video scenarios reported by the user. Those scenarios are now explicit regression checks and were visually rechecked. No unresolved difference was observed in these tested states; this is not a claim that all annotation workflows or browser versions are bug-free. Different video content, the floating toolbar location requested by the user, and omission of unsupported Preview features are intentional. Follow-up P3: broader app-level icon and layout refinements can continue against native macOS references; this change establishes shared tokens and editing conventions.

final result: passed

## 2026-09-06 · Light frost and transport follow-up

- Chrome uses 6px background blur; menus and the fixed palette use 8px blur with an 88% tint. The annotation toolbar was reduced from 12px blur. Only bounded chrome/popover surfaces are filtered; no viewport-sized effect, saturation pass, filter animation, or persistent will-change layer. Color tiles and content remain opaque. Reduced transparency/high contrast disables blur and uses solid fills.
- Transport: 36px row, centered 28px targets, 18px icons, consistent 12px time typography, 2px playback-button gaps and 8px group spacing. Reduced excessive time-input caret padding. The independent visibility button shares the row center and outer inset. Removed the annotation button and its event binding; direct stage drawing, N shortcut and annotation panel remain available.
- Built-CSS verification caught a prefix-order issue: minification retained only the prefixed backdrop-filter and Chromium showed no blur. The standard declaration now follows the prefixed fallback; both browser regressions assert the actual computed filter.
- WebKit and Chromium menu/transport regressions passed: 1280/600/480/360/280/240px widths, equal time fonts, vertical centers, no overlap, minimum 36px seek area, menu toggles/tooltips, palette behavior and high-contrast filter removal. Existing WebKit annotation and workspace/source regressions passed. Build and diff whitespace checks passed. Manual in-app checks covered the palette and video filling the area behind the transport.
- Local WebKit headless DPR 2, four 1080p tracks (AV1/VP9/H.264/HEVC), four 4-second alternating runs with the action menu open: no blur / blur / blur / no blur all passed. Playback speed 0.988–0.990×; worst-track P95 frame gap 33.34–33.60ms. This measures app presentation timing on this machine, not GPU cost, physical scanout or low-end hardware. Evidence: `/tmp/voidplayer-frost-perf.jsonl`. Annotation editing closes when playback starts, so this comparison opens the action menu after playback starts.


## 2026-09-06 · Actual transport spacing and compact annotation wrapper

The previous geometry check caught overall alignment but missed rendered text whitespace and a higher-specificity global button padding rule. The root-scoped `--time-input-width` had already expanded the old 9-character/4px-padding/1ch-caret values, so changing descendant tokens did not update it. Position width now resolves its character count directly on the input with zero horizontal padding and one caret pixel, using current time length rather than duration length. Slash gaps are 4px. All transport action targets are 28px with the same 5px padding, including hover/focus states and the independent eye button; playback/right-action spacing is now 4px. Phosphor glyph whitespace is optically balanced within their shared SVG boxes. Annotation wrapper padding is reduced from 8px to 4px with a 9px radius.

Build, whitespace check, and WebKit/Chromium menu regressions passed. The browser checks now measure input width against actual font text width (less than 2px spare), and compare all five transport buttons' sizes/padding across 1280–240px widths. Manual in-app screenshot verified the compact `current / duration` group and thinner annotation wrapper. No playback/decoder path changed in this follow-up.
