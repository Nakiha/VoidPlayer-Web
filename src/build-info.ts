// Replaced by Vite for both dev and production. Node unit tests use null.
export const buildInfo: { revision: string; builtAt: string; sourceDigest: string; wasmDigests: Record<string, string | null> } | null = null;
